name: State Transition Example

on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  check_suite:
    types: [completed]

jobs:
  transition:
    runs-on: ubuntu-latest
    steps:
      - name: Explain intent
        run: |
          echo "This is a template workflow."
          echo "Use gh api or actions/github-script to:"
          echo "1) Detect linked issue"
          echo "2) Update labels/project fields"
          echo "3) Post evidence summary comment"
